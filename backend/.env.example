# backend/.env.example

# Server Configuration
PORT=
NODE_ENV=

# Database Configuration (match these to the Kubernetes deployment)
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=
# DB_PASSWORD is set via Kubernetes secret, not in .env file

# JWT Configuration
JWT_SECRET=

# CORS Configuration (adjust based on the domain)
# CORS_ORIGIN=https://yourdomain.com