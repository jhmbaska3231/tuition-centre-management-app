# backend/.env.example

# Database Configuration (these will be overridden by Kubernetes)
DB_HOST=
DB_PORT=
DB_USER=
DB_PASSWORD=
DB_NAME=

# JWT Configuration
JWT_SECRET=

# Server Configuration
PORT=
NODE_ENV=

# CORS Configuration (adjust based on the domain)
FRONTEND_URL=